server:
  port: 8100

spring:
  application:
    name: auth-server
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev

encrypt:
  key-store:
    location: classpath:mykeystore.jks
    alias: mykey
    password: password

